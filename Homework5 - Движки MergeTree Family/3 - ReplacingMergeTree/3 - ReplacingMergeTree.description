Движок ReplacingMergeTree – это движок, реализующий  дедупликацию через удаление дублирующихся записей с одинаковым значением ключа сортировки (столбец объявленный в секции ORDER BY).
В нашем случае – это поля id и status.