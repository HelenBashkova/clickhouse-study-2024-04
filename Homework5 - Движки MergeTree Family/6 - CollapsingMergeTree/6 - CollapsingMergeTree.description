CollapsingMergeTree- это тип таблицы, который предназначен для хранения данных в упорядоченном виде и поддерживает эффективное слияние данных при добавлении новых записей. 
Этот тип таблицы особенно полезен для случаев, когда необходимо хранить временные ряды или данные, которые можно эффективно сжимать и агрегировать. 

После выполнения команды (SELECT * FROM tbl6 FINAL) будут исключены дубликаты через логику сворачивания (удаления) строк.
Асинхронно удалится пара строк, если все поля в ключе сортировки (в нашем случае id, status) эквивалентны, за исключением специального поля Sign,
которое может принимать значения 1 и -1. Строки без пары сохраняются. 
Таким образом на последнем скриншоте видно, что в таблице останется 1 запись. 
